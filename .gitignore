# Claude Code / tooling
.claude/

# Python (any local scripts/experiments)
.venv
venv
env

# Secrets — never commit
.env
.env.*
.dev.vars
.dev.vars.*

# Node / v2 + worker build artifacts
node_modules/
dist/
.astro/
.wrangler/
*.tsbuildinfo
npm-debug.log*
yarn-error.log*

# v1 permalink bridge — copied into public/ fresh at deploy time by
# .github/workflows/deploy.yml, never committed (see that file's comment)
public/posts/
public/posts-data/
public/posts.json
public/rss.xml

# Editors
.vscode/
.idea/
*.swp
*~

# OS
.DS_Store
Thumbs.db
